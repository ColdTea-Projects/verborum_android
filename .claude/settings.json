{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && [ -f \"$f\" ] && git -C \"$(dirname \"$f\")\" add -- \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Staging new file to git"
          }
        ]
      }
    ]
  }
}
