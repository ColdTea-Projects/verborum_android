<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- AppCompat-based: AppCompatActivity requires it, and it is what carries per-app locales
         below API 33. DayNight keeps the window background in step with the Compose theme. -->
    <style name="Theme.Verborum" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
